const add = (x,y) => {
    return x+y
}

const sub = (x,y) => {
    return x - y;
}

const name = "Ayush"

/*
module.exports.add = add;
module.exports.name = name;
module.exports.sub = sub;
*/

module.exports = { add, name, sub}
