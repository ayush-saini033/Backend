// each file is a module in node

// const oper = require('./operator')

const {add, sub, name} = require('./operator')

// opr -> it is a object

console.log(add(5,5))
console.log(sub(4,5))
console.log(name)
